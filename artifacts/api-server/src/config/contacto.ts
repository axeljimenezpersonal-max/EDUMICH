/**
 * Contacto institucional para los correos que envía el sistema — FUENTE ÚNICA
 * del lado del servidor (el portal tiene su gemelo en `lib/contacto.ts`).
 *
 * PROVISIONAL: `prepa@modula22.mx` mientras se habilita el buzón definitivo.
 * Las plantillas que reciben un contacto por parámetro siguen mandando; esto es
 * el respaldo cuando no viene ninguno.
 */

export const CONTACTO_CORREO = 'prepa@modula22.mx';
export const CONTACTO_TELEFONO = '+52 443 322 9250';
